using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StartScript : MonoBehaviour
{
    public string boss;

    // Start is called before the first frame update
    void Start()
    {
        string[] boss = new string[5];
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
